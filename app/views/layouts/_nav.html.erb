<header role="banner">
  <nav>
    <div class="container grid">
      <div class="unit three-fifths">
        <h1>Welcome to the Grand Send Station</h1>

      </div>

        <div class="user_edit">
          <% if session[:user_id] %>
          <%= render 'layouts/logged' %>
          <% end %> |
          <%= link_to 'Routes', '/ascends' %> | <%= link_to 'Main', '/users' %> |
          <% if session[:user_id] %>
          <%= link_to 'Logout', "/logout" %>
          <% end %>
        </div>
      <!--
        <ul>
          <li>Home</li>
          <li>Contact</li>
          <li>Profile</li>
        </ul>
      -->
    </div>
  </nav>
</header>
